const { executeQueryEmployeeAccessDB } = require('../../../config/db');

module.exports = {
  pm_start_show: async (req, res) => {
  try {
        const sqlVehicle = `
        SELECT v.reg_id, v.reg_numder 
        FROM Truck_vehicle_registration v
        
         `
  } catch (error) {

  } 
},
};
